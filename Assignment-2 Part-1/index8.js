// 8. Write a JS code to find the largest number in an array
let num=[12,13,15,16,18,20];
for(let i=0;i<num.length;i++)
{
    if(num[i]>num.length){
        console.log(`Number is Greater ${num[i]} `);
    }
}